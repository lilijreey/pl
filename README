## 高性能Linux 网络服务框架库

### 特性
* 高稳定
* 高可伸缩
* 高性能 10W/qps
* 纯一步，同步写法
* 支持协程模式
* 事件驱动


pl {
 os {
   net{
    socket { }
    tcp_server{}
   }
   thread { }
  }
 log { }
 config { }
}




TcpServer svr("IP", "Port");
svr.setOpt();
svr.setSocketOpt();
svr.listen();
svr.closeListen();
